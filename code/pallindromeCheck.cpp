/* pallindromes are strings which read the same forwards and
 * backwards. the challenege here is to pick pallindromes from
 * a series of lucky strings.*/
#include <iostream>
using namespace std;
int main() {
	int tests;
	cin >> tests;
	return 0;
}
